# Subtask 2.2 — Sign-in (and sign-up) screens

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.2**.
2. Read `CLAUDE.md` for project conventions and commands.

Auth uses PocketBase via `src/contexts/AuthContext.tsx`. `SignInScreen` is wired into `src/App.tsx`; `SignUpScreen.tsx` exists but is currently not wired in — restyle it anyway for consistency, do not change its wiring.

**Scope note (from master plan):** guest mode ("Fortsæt som gæst" in the design) is **out of scope** — do not implement it; omit that button.

**Depends on:** subtasks 1.1, 1.2, 1.3.

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isSignin }}">` block — circular back button (42px, surface, `--line` border), Spectral ~34px h1 "Velkommen tilbage", subtitle, uppercase Space Mono field labels, 54px/14px-radius inputs with Spectral value text, the password field's eye icon, and the bottom-anchored primary CTA "Log ind". Extract exact values and copy.

## Task

1. **`src/components/SignInScreen.tsx`:** restyle to the design using 1.3 primitives:
   - Back button (top-left circle) → WelcomeScreen (keep existing callback prop).
   - Heading + subtitle per design copy.
   - Email + password fields with MonoLabel labels and the new input style; add a password-visibility toggle (eye icon) as shown.
   - Bottom-anchored primary "Log ind" button; keep existing submit/loading/error behavior — restyle the error display with new tokens (e.g. accent-tinted text/panel).
   - Remove old green gradient background; use `--bg` (optionally the subtle topo-SVG treatment if it fits).
2. **`src/components/SignUpScreen.tsx>`:** apply the same visual recipe (labels, inputs, buttons, heading style) so it's consistent if re-enabled later. Do not wire it into `App.tsx`.
3. Both themes + safe-area insets supported.

## Files

- `src/components/SignInScreen.tsx`
- `src/components/SignUpScreen.tsx`

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Sign-in matches the design (minus the guest button) in both themes; login works against PocketBase exactly as before, including error and loading states.
- No changes to AuthContext or API code.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
