# Subtask 3.4 — PWA update toast

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 3, subtask 3.4**.
2. Read `CLAUDE.md` for project conventions and commands.

Service-worker updates use vite-plugin-pwa with `registerType: 'prompt'`; the UI is `src/components/PWAUpdatePrompt.tsx` (plus `useManualUpdateCheck` hook). **Keep the update/reload logic; restyle the presentation.**

**Depends on:** subtasks 1.1–1.2.

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isUpdate }}">` block — toast card pinned near the top (below the status bar/safe area), `var(--ink)`-colored background (dark card in light theme), 16px radius, `ss-fade` in, refresh icon in a translucent 38px tile, Spectral 15px "Ny version tilgængelig" + 12px subline, and a compact accent "Opdatér" button on the right.

## Task

1. Restyle `PWAUpdatePrompt.tsx` to the design's toast card. "Opdatér" triggers the existing SW `updateServiceWorker`/reload flow; keep any dismiss affordance the current component has (add a subtle close if none, so users aren't forced to update).
2. Position: top of viewport under the safe area, above app chrome (z-index over the floating bar), `ss-fade` entrance.
3. Both themes (note: the card is ink-colored, so it naturally inverts — verify contrast in dark mode where `--ink` is light; if that flips the card light, use an explicit dark-surface token pairing per the design's dark-mode intent).

## Files

- `src/components/PWAUpdatePrompt.tsx`

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Toast matches the design; tapping "Opdatér" activates the waiting service worker and reloads; app remains usable if the toast is ignored/dismissed.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
