# Subtask 2.7 — Add/edit spot sheet

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.7**.
2. Read `CLAUDE.md` for project conventions and commands.

Creation/editing is `src/components/AddEditModal.tsx` (shadcn Dialog, react-hook-form + Zod from `src/lib/schemas.ts`), with image capture (`ImageCapture.tsx`, client-side compression) and location picking (`LocationPickerModal.tsx`). Offline creation queues via the pending-spots IndexedDB layer — none of that logic may change.

**Depends on:** subtasks 1.1–1.3 (sheet primitive, inputs, TypeBadge, MonoLabel, completed Danish labels).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isAdd }}">` block — sheet chrome (max-height 92%), header row (Spectral 23px "Nyt fund" + 36px circular close button), "Vælg art" type grid (4 columns, square aspect-ratio gradient tiles with 16px radius, PNG icon, selected tile ringed with 3px accent border — see `typeGrid`/`pickIdx` logic in the script, 10.5px labels below), "Placering" row (52px surface field, pulsing brand dot, Space Mono coordinates, "Nuværende" hint), "Noter" textarea (min-height 88px, Spectral placeholder), "Tilføj foto" secondary button with camera icon, and the accent "Gem fund" CTA.

## Task

1. Convert `AddEditModal.tsx` from a centered Dialog to the 1.3 bottom-sheet primitive with the design's chrome. Title: "Nyt fund" for create, an equivalent Danish edit title ("Redigér fund") for edit mode.
2. **Type picker:** replace the current type selection UI with the 4-column grid of all 25 `FORAGING_TYPES` (gradients/icons via `getForagingSpotConfig`, Danish labels via `getDanishLabel` — completed in 1.3). Selected tile gets the accent ring. Grid scrolls within the sheet.
3. **Placering row:** surface field showing the chosen coordinates in Space Mono with the pulsing brand dot; label "Nuværende" when it's the live GPS position, otherwise indicate manually-picked. Tapping opens the location picker (`LocationPickerModal` today; redesigned separately in 2.9 — keep the contract).
4. **Noter** textarea with the new input style and the design's placeholder copy.
5. **Tilføj foto** secondary button hooked to the existing `ImageCapture` flow; show captured/selected thumbnails (restyle whatever thumbnail strip exists today with new tokens).
6. **Gem fund** accent CTA; keep react-hook-form + Zod validation, error display (restyled), submit/loading states, optimistic updates, and offline pending behavior exactly as they are.
7. Handle the keyboard: sheet content must remain scrollable and the CTA reachable with the on-screen keyboard open (test in responsive mode).
8. Both themes + safe areas.

## Files

- `src/components/AddEditModal.tsx`
- `src/components/ImageCapture.tsx` (token restyle only if its UI shows inside the sheet)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Sheet matches the design in both themes; all 25 types selectable with Danish labels.
- Create + edit flows work end-to-end (online and offline-queued), validation and image attach unchanged functionally.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
