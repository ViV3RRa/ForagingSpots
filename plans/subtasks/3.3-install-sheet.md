# Subtask 3.3 — PWA install sheet

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 3, subtask 3.3**.
2. Read `CLAUDE.md` for project conventions and commands.

Install UX is `src/components/PWAInstallPrompt.tsx`: iOS shows manual "Share → Add to Home Screen" instructions (after a delay), Android/Chrome uses the `beforeinstallprompt` event, with session-storage dismissal. **Keep all platform detection and dismissal logic; restyle the presentation.**

**Depends on:** subtasks 1.1–1.3 (sheet primitive).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isInstall }}">` block — bottom sheet with grab handle; header row (64px brand-green app-icon tile containing the mini badge cluster, Spectral 21px "Føj til hjemmeskærm", Space Mono domain subline); three benefit cards (surface, 14px radius, brand-colored icons: "Åbner lynhurtigt" / "Virker offline" / "Fuld skærm"); accent "Installér app" CTA; then an "på iPhone" divider and the iOS instruction row (inline share-icon chip + "og vælg **Føj til hjemmeskærm**").

## Task

1. Rebuild `PWAInstallPrompt.tsx` on the 1.3 sheet primitive with the design's layout.
2. Platform behavior mapped onto the design:
   - **Android/Chrome** (`beforeinstallprompt` captured): show the sheet with the "Installér app" CTA wired to `prompt()`; the iOS divider/instruction section hidden.
   - **iOS Safari:** no CTA (it can't trigger install) — show the sheet with the benefit cards and the iOS instruction row prominent instead.
   - Keep the existing show-delay and session-dismiss rules; sheet scrim tap and swipe/close dismiss for the session.
3. App-icon tile: recreate the mini badge-cluster icon in JSX/SVG using the existing spot-type PNGs (chanterelle/blueberry/porcini per the design) — coordinate with subtask 3.5 which generates real manifest icons from the same concept.
4. Both themes + safe areas.

## Files

- `src/components/PWAInstallPrompt.tsx`

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Sheet matches the design in both themes; Android path triggers the native install prompt; iOS path shows instructions; dismissal persists for the session as before.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
