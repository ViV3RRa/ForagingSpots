# Subtask 2.10 — Photo lightbox

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.10**.
2. Read `CLAUDE.md` for project conventions and commands.

The image lightbox is `src/components/ImageViewer.tsx`, opened from the detail drawer's gallery (subtask 2.6). Full-size image URLs come from `getSpotImageUrls` in `src/lib/pocketbase.ts`.

**Depends on:** subtasks 1.1–1.2; coordinates with 2.6 (which invokes it).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isPhoto }}">` block — near-black `#141009` fullscreen backdrop with `ss-fade`, top row (Space Mono "1 / 3" counter left, 40px circular translucent close button right), centered image card (3:4 aspect, 20px radius, large shadow) with a bottom gradient caption (Spectral 19px spot name + Space Mono date · coordinates line), and the bottom thumbnail strip (58px, 12px radius, active thumb = accent 2px border + full opacity, inactive dimmed — see `photoThumbs` logic).

## Task

1. Restyle `ImageViewer.tsx` to the design: backdrop, counter, close button, rounded image presentation with the caption overlay (spot name via `getDanishLabel`, date and coordinates from the spot — pass them in as props from the detail drawer if not already available).
2. Thumbnail strip for navigation with the active/inactive treatment; keep any existing swipe/arrow navigation behavior.
3. The backdrop color is intentionally theme-independent (dark in both themes) — no `.dark` variants needed except verifying token-based bits.
4. Handle single-image spots (hide strip or show single thumb) and loading states for full-size images.

## Files

- `src/components/ImageViewer.tsx`
- `src/components/PinDetailsDrawer.tsx` (only to pass name/date/coords props if missing)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Lightbox matches the design; navigation between images works via thumbnails (and swipe if previously supported); close returns to the drawer.
- Caption shows correct Danish label, date, and coordinates.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
