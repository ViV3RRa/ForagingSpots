# Subtask 2.4 — Map view (pins, clusters, locate, user dot)

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.4**.
2. Read `CLAUDE.md` for project conventions and commands.

The map is `src/components/MapView.tsx`: react-map-gl + Supercluster clustering, custom markers built from `getForagingSpotConfig` (`src/components/icons/index.tsx`), user-location tracking with a GPS-follow mode, and a locate button.

**Depends on:** subtasks 1.1–1.4 (tokens, fonts, TypeBadge, custom map styles) and 2.3 (chrome, so controls don't collide).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study, inside the `isApp` block:** the "pins" `<sc-for>` (52px circular gradient badge, 3px `--pin-ring` border, inner white highlight, drop shadow, 2×9px stem below, anchored `translate(-50%,-100%)`), the "current location" dot (16px `--brand` circle, 3px ring, `ss-pulse` animation), and the "LOCATE" button (52px circle, position, and the active-state color inversion — see `locateBg`/`locateInk` in the script's `renderVals()`: active = brand bg in light / gold in dark).

## Task

1. **Markers:** rebuild spot markers using `TypeBadge` (from 1.3) at 52px with the stem, anchored at the pin tip. Tap still opens the spot detail (keep existing `onSpotClick`/selection contract).
2. **Clusters:** the design shows no cluster treatment — derive one consistent with the language (e.g. surface or `--brand` circle, `--pin-ring` border, Spectral 600 count). Keep Supercluster behavior (tap = zoom/expand) unchanged.
3. **User-location dot:** brand-colored with `--pin-ring` border and the `ss-pulse` keyframe (available from 1.1).
4. **Locate button:** 52px circle positioned above the FAB per the design; inactive = surface bg + brand icon, active GPS-follow = inverted colors per the design logic. Keep existing follow-mode behavior and fly-to animation.
5. Remove old marker/control styling that's now dead; keep the compass/bearing behavior if present (restyle minimally to match).
6. Verify markers and dot render correctly on both the light and dark map styles.

## Files

- `src/components/MapView.tsx` (+ any marker subcomponents it imports)
- `src/components/TypeBadge.tsx` (consume; extend with an optional stem/pin variant if cleaner)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Pins, clusters, user dot, and locate button match the design language in both themes; locate active-state inversion works.
- Clustering, spot selection, GPS follow, and fly-to-spot all behave exactly as before.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
