# Subtask 2.1 — Welcome screen

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.1**.
2. Read `CLAUDE.md` for project conventions and commands.

The app is a Danish foraging-spots PWA (React 19 + TypeScript + Vite, Tailwind CSS, shadcn/ui). Navigation is state-driven in `src/App.tsx` (no router): Welcome → SignIn → main app.

**Depends on:** subtasks 1.1 (tokens/theme), 1.2 (typography), 1.3 (primitives — buttons, TypeBadge, MonoLabel).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isWelcome }}">` block — background topo-curve SVG, the overlapping icon-badge cluster (positions, sizes, gradients, z-order of chanterelle/blueberry/porcini/raspberry badges), the Space Mono kicker "Din personlige", the 44px Spectral title, body copy, and the two bottom buttons. Extract exact sizes, colors, spacing, and copy.

## Task

Rebuild `src/components/WelcomeScreen.tsx` to match the design:

1. Decorative SVG background curves using `--map-land` / `--map-water` tokens (see the design's SVG paths; adapt to full-viewport instead of the 390×838 mock frame).
2. Icon cluster illustration: four overlapping circular gradient badges with PNG icons (use `TypeBadge` from 1.3 or absolutely-positioned equivalents with `--pin-ring` borders and drop shadows).
3. Text block: MonoLabel kicker, Spectral 600 ~44px two-line title "Skovens Skatte", supporting paragraph (keep the design's Danish copy).
4. Bottom actions: primary button "Kom i gang" and text button "Jeg har allerede en konto" — both navigate to the sign-in screen (preserve the existing `onNavigateToSignIn`-style callback props; check current props in the file and in `App.tsx`).
5. Remove the old TreePine/Leaf illustration, green gradients, and the inline PWA install prompt if it visually clashes (keep its logic mounted — install UX is restyled in subtask 3.3).
6. Respect safe-area insets (existing utility classes) and support both themes.

## Files

- `src/components/WelcomeScreen.tsx`
- `src/App.tsx` (only if prop wiring must change)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Welcome screen matches the design in light and dark themes on a mobile viewport (390px wide) and scales sensibly on desktop.
- Both buttons still navigate to sign-in; no auth logic changed.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
