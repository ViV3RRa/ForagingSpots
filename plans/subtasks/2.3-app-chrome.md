# Subtask 2.3 — App chrome (floating top bar, view toggle, FAB, location chip)

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.3**. It is the frame around the map and list views; subtasks 2.4, 2.5, 2.8 render inside/from it, so this lands before them.
2. Read `CLAUDE.md` for project conventions and commands.

The authenticated shell is `src/components/MainMapScreen.tsx`, which composes `TopBar.tsx`, `MapView.tsx`/`SpotListView.tsx` (view toggle state), `FilterButton.tsx`, and `FloatingActionButton.tsx`. Navigation is React state, no router.

**Depends on:** subtasks 1.1, 1.2, 1.3.

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study, inside the `isApp` block:** the "floating top bar" comment section (search field 48px + filter square button 48px + circular brand avatar 48px, top gradient scrim from `--bg` to transparent), the "bottom toggle" (Kort/Liste pill — note active-tab colors via `tabOn`/`tabInk` in the script, different between light and dark), the "location chip" (bottom-left, map only), the FAB (60px accent circle), and their exact positions/spacings/shadows.

## Task

1. **`TopBar.tsx` → floating bar** over the content: search field (surface bg, `--line` border, 14px radius, Spectral placeholder "Søg blandt fund…"), square filter icon button (absorb/replace `FilterButton.tsx` here if cleaner), circular `--brand` avatar showing the user's initial, opening the existing user dropdown menu. Gradient scrim behind so map content fades under it. Respect safe-area top.
   - Search currently lives inside `SpotListView`. Decide the cleanest wiring: lift search state to `MainMapScreen` so the floating bar drives both list filtering and (on map view) either filters pins or is display-only until focused. Minimum requirement: list search keeps working; do not regress it.
2. **Theme toggle:** add light/dark/system selection to the avatar dropdown menu, wired to the ThemeContext from 1.1. Keep the existing logout item.
3. **Bottom view toggle:** centered pill (surface bg, `--line` border, 16px radius, 5px padding) with "Kort"/"Liste" Spectral 600 tabs; active tab filled per the design's light/dark logic. Replace the current view-toggle UI (check where it lives — `TopBar` or `MainMapScreen`).
4. **FAB:** restyle `FloatingActionButton.tsx` — 60px accent circle, bottom-right, thin "+" glyph, accent glow shadow, above safe-area bottom.
5. **Location chip** (map view only, bottom-left): surface chip with brand dot + Space Mono text. The design shows a locality name ("Silkeborg Skov"); the app has no reverse geocoding — show nothing or the user's coarse coordinates instead (pick one; note the choice in code). Skip entirely if user location is unavailable.
6. Keep z-ordering sane with sheets/drawers that overlay the chrome.

## Files

- `src/components/TopBar.tsx`, `src/components/MainMapScreen.tsx`, `src/components/FloatingActionButton.tsx`, `src/components/FilterButton.tsx` (possibly removed/absorbed)
- `src/contexts/ThemeContext.tsx` (consume only)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Chrome matches the design in both themes: floating search bar, avatar menu (with working theme toggle + logout), Kort/Liste pill switching views, accent FAB opening the add flow, filter button opening the filter UI.
- List search and all existing menu actions still work; safe areas respected in iOS standalone mode.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
