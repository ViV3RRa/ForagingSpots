# Subtask 2.14 — Map cluster marker & compass button

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.14**.
2. Read `CLAUDE.md` for project conventions and commands.

Subtask 2.4 migrated `src/components/MapView.tsx`, but two elements had no design and were
derived: the Supercluster cluster marker and the reset-north compass button. The design file now
contains canonical designs for both (added 2026-07-08 via
`plans/claude-design-brief-missing-elements.md`). **Do not change clustering or camera logic —
visuals only.**

**Depends on:** 1.1–1.4 and 2.4 (all done).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** in the `<sc-if value="{{ isMap }}">` block, the
  "cluster marker" comment — 52px `--brand` circle, Spectral 600 19px `--brand-ink` count, 3px
  `--pin-ring` border, `0 4px 12px rgba(0,0,0,.35)` shadow (no inner white inset), plus a halo
  ring (absolute, inset −6px, 1.5px `--brand` border at .3 opacity). And the
  `<sc-if value="{{ isCompass }}">` block — 52px surface circle stacked above the locate button,
  containing a custom compass-rose SVG (outer circle in `--line`, north needle filled `--accent`,
  south needle filled `--muted`) that rotates with the map bearing, and a fixed Space Mono 8px
  700 accent "N" letter at the top of the circle (the letter does not rotate). Jump-rail state
  "Kompas" previews it.

## Task

1. **Cluster marker:** update the cluster rendering in `MapView.tsx` to the design (colors,
   typography, border, shadow, halo ring). Keep click-to-expand-zoom behavior and the hover
   scale if desired.
2. **Compass:** replace the lucide `Compass` icon with the design's compass-rose SVG; the rose
   rotates with the current bearing (needle points north), the "N" stays fixed. Keep the
   existing show-when-rotated / fade / reset-to-north behavior and positioning relative to the
   locate button (design stacks it 60px above).
3. Both themes (halo/needles must read on both map styles).

## Files

- `src/components/MapView.tsx`

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Clusters match the design in both themes; tapping still expands.
- Rotating the map shows the design's compass with a correctly-pointing needle; tapping resets
  north; it hides when bearing returns to 0.
- Pins, user dot, locate button, and follow mode unchanged.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
