# Subtask 2.11 — Delete confirmation dialog

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.11**.
2. Read `CLAUDE.md` for project conventions and commands.

The reusable confirmation dialog is `src/components/ConfirmationDialog.tsx` (shadcn Dialog), used at least for spot deletion from the detail drawer. Keep it generic — other confirmations may use it.

**Depends on:** subtasks 1.1–1.3.

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isDelete }}">` block — centered card (24px radius, 34px page padding, `ss-fade`, scrim `rgba(20,15,8,.5)`), 60px circular tinted icon (`delTint` — accent at low opacity, different per theme; see script), Spectral 22px title "Slet dette fund?", body copy quoting the spot name ("… fjernes permanent fra din skovbog. Dette kan ikke fortrydes."), and stacked buttons: accent-filled "Slet fund" over outlined "Annullér".

## Task

1. Restyle `ConfirmationDialog.tsx` to the design's card: icon slot (default: trash in the tinted circle), title, description (supporting a highlighted subject name like the design's quoted spot name), stacked confirm/cancel buttons using 1.3 variants (confirm = accent, cancel = outline).
2. Keep the component API generic (title/description/confirm-label/cancel-label/onConfirm props — extend minimally if the icon or subject-name slot needs adding). Update call sites for any API additions.
3. Verify the delete flow copy from the detail drawer matches the design's Danish text.
4. Both themes.

## Files

- `src/components/ConfirmationDialog.tsx`
- Call sites (e.g. `src/components/PinDetailsDrawer.tsx`) if props change

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Dialog matches the design in both themes; deleting a spot still works (including optimistic/offline behavior); cancel closes without action.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
