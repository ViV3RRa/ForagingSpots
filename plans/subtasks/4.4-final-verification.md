# Subtask 4.4 — Final verification & QA

## Context

You are implementing the final subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan. This file is **Phase 4, subtask 4.4** — run last, after every other subtask is complete.
2. Read `CLAUDE.md` for project conventions and commands (dev server, PocketBase backend startup).

## Design source of truth (for visual comparison)

- Link: https://claude.ai/design/p/67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "67e5a51f-04a0-4dc5-9ca9-0d4bf9a807b3"`, `path: "Skovens Skatte.dc.html"`. Compare each implemented screen against its design block as you QA (screens are `<sc-if>` blocks; light/dark palettes in the script's `LIGHT`/`DARK` constants).

## Task

1. **Build gates:** `npm run lint`, `npm run build`, then `npm run preview`. Zero errors.
2. **Lighthouse:** run against the preview build (mobile) — PWA installable, no manifest errors; note performance score (image/font weight should be healthy after 4.3/1.2).
3. **Manual QA script** — with PocketBase running (`./pocketbase serve` from `pocketbase/`), on a mobile viewport, in **both themes**:
   - Welcome → sign-in (bad + good credentials) → permission priming screen (fresh profile) → map.
   - Map: pins render, cluster tap zooms, locate button + follow mode, user dot pulse, theme toggle from avatar menu.
   - List: search, sort, relative dates, distance values, tap → detail. Empty state on a fresh account.
   - Detail drawer: gallery 0/1/2/3+ images, lightbox navigation, coordinates "Redigér" → location editor (drag, confirm, cancel), share/unshare, edit, delete (confirm dialog).
   - Add flow: FAB → type grid (all 25 types, Danish labels), location row → editor, notes, photo capture/attach, save; verify the new spot on map + list.
   - Filter sheet: category tabs, species toggles, reset, apply — affects map and list.
   - Offline: devtools offline → banner appears, cached map tiles/spots visible, create a spot offline → pending badge, go online → syncs.
   - PWA: install prompt sheet (Android emulation + iOS instructions variant), update toast (bump SW to trigger, or verify via dev tooling), installed-app icon/splash.
   - Theme: system-change follows live; manual override persists across reload.
4. **Fix or file:** fix small findings directly; for anything larger, append a "Findings" section to this file with repro steps so follow-up work is captured.
5. Summarize results (pass/fail per QA area) in the final report/commit message.

## Acceptance criteria

- All build gates green; Lighthouse PWA checks pass.
- Every QA area above verified in both themes with no functional regressions against pre-migration behavior.
- Any unfixed findings documented in this file under "Findings".

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
