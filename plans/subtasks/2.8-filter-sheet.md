# Subtask 2.8 — Filter sheet

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.8**.
2. Read `CLAUDE.md` for project conventions and commands.

Filtering is `src/components/FilterDialog.tsx`, opened from the filter button in the app chrome (subtask 2.3), with filter state living in `MainMapScreen.tsx` and applied to both `MapView` and `SpotListView`. Keep that state contract.

**Depends on:** subtasks 1.1–1.3 and 2.3.

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isFilter }}">` block — sheet chrome (max-height 86%), header ("Filtrér" + accent "Nulstil" reset link), "Kategori" segment of three equal buttons (Alle/Svampe/Bær, active = accent fill — see `catOn`/`catInk` logic), "Arter" checklist rows (44px gradient badge, Spectral 17px name, 26px square checkbox with 8px radius — checked = accent fill with ✓, unchecked = transparent with `--line` border; see `filterTypes` logic), and the footer accent CTA "Vis resultater" above a `--line2` top border.

## Task

1. Rebuild `FilterDialog.tsx` as a bottom sheet (1.3 primitive) with the design's header/footer.
2. **Category segment (new):** add a mushroom/berry categorization for `FORAGING_TYPES` in `src/utils/foragingTypes.ts` (e.g. `getForagingCategory(type): 'mushroom' | 'berry' | 'other'`). Category selection bulk-filters: "Svampe" limits to mushroom types, "Bær" to berries, "Alle" clears. Decide how `other` behaves (include under Alle only, or under both) and note the choice in code.
3. **Species checklist:** rows per the design driven by the types present in the user's spots (fall back to all types if that's simpler with the existing filter-state shape — but prefer present-types, matching the mock's `filterTypes` behavior). Checkbox toggles per type.
4. **Nulstil** resets categories + species to all-on; **Vis resultater** applies and closes. Preserve the exact filter-state contract consumed by `MapView`/`SpotListView` (adapt internally rather than changing consumers, unless a tiny extension is needed for categories).
5. Both themes + safe areas.

## Files

- `src/components/FilterDialog.tsx`
- `src/utils/foragingTypes.ts` (category map)
- `src/components/MainMapScreen.tsx` (only if filter-state shape needs a compatible extension)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Sheet matches the design in both themes; category tabs and per-species checkboxes filter both map pins and list rows correctly; reset works.
- No regressions in how filters combine with search/sort.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
