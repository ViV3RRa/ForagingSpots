# Subtask 3.2 — Offline banner & pending-sync badge

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 3, subtask 3.2**.
2. Read `CLAUDE.md` for project conventions and commands.

Offline support is a core recent feature (IndexedDB pending queue, `useNetworkStatus`, `usePendingSpots` hooks). The visible surfaces are `src/components/OfflineBanner.tsx` (banner + pending count) and `src/components/PendingSyncBadge.tsx` (per-spot badge). **Visual restyle only — do not touch sync logic.**

**Depends on:** subtasks 1.1–1.2 and 2.3 (banner sits below the floating top bar).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** the `<sc-if value="{{ isOffline }}">` block — floating card below the top bar (14px radius, `ss-fade` in), wifi-off icon, Spectral 14px title "Offline · viser gemte fund" + 11.5px subline "Nye fund synkroniseres, når du er online igen." **Note the dedicated amber palette** in the script's `renderVals()`: `offlineBg`, `offlineBorder`, `offlineInk`, `offlineInk2` — different values for light and dark themes; extract all eight hex values.
- **Pending-sync indicators are now canonically designed too** (added 2026-07-08): study the
  `spot.syncPending`/`spot.syncFailed` chips in the `isList` rows ("Afventer synk" /
  "Synk fejlede" — Work Sans 11px 600, padding 3px 9px, 8px radius, clock/warning icons, colors
  `pendBg`/`pendInk`/`failBg`/`failInk` from `renderVals()` per theme), the long-copy variants in
  the `showDetail` header ("Afventer synkronisering" / "Synkronisering fejlede"), and the map-pin
  corner indicator (18px circle at −3px/−3px on the pin, 2px `--pin-ring` border, solid
  `pendInk`/`failInk` disc with a light clock/✕ glyph). Jump-rail state "Afventer" previews the
  detail variant.

## Task

1. Restyle `OfflineBanner.tsx` to the floating card design, positioned under the floating top bar (coordinate z-index/offsets with the 2.3 chrome). Use the exact amber palette per theme (add the values as tokens in `tokens.css` if that's cleaner, e.g. `--offline-bg` etc.).
2. Keep the pending-count information the current banner shows — fold it into the subline (e.g. append "· N fund venter") when count > 0, keeping the design's copy otherwise.
3. Restyle `PendingSyncBadge.tsx` to the design's chips: short copy ("Afventer synk" /
   "Synk fejlede") by default with a prop/variant for the detail drawer's long copy
   ("Afventer synkronisering" / "Synkronisering fejlede"). Use the exact `pendBg`/`pendInk`/
   `failBg`/`failInk` values per theme (share the amber tokens with the banner and the 2.17
   offline-lock notice).
4. Restyle `PendingSyncIcon` (map-pin corner indicator, same file) to the design's 18px disc.
   Callers in `SpotListView`, `MapView`, and `PinDetailsDrawer` should need no changes beyond a
   possible variant prop.
5. Both themes; `ss-fade` entrance; banner must not overlap the search bar or block map interaction beyond its own bounds.

## Files

- `src/components/OfflineBanner.tsx`
- `src/components/PendingSyncBadge.tsx`
- `src/styles/tokens.css` (optional offline tokens)

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- Toggle devtools offline: banner appears styled per design in both themes with correct amber palette; pending count shows when spots are queued; badge styling matches.
- No changes to network/sync behavior.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
