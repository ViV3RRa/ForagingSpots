# Subtask 2.16 — Top-bar states: filter indicator & photo avatar

## Context

You are implementing one subtask of the "Skovens Skatte" UI redesign migration. Before starting:

1. Read `plans/new-design-migration.md` — the master plan (design language, token tables, scope, screen-to-component map). This file is **Phase 2, subtask 2.16**.
2. Read `CLAUDE.md` for project conventions and commands.

Subtask 2.3 migrated `src/components/TopBar.tsx`, but two states were provisional: the
"filters active" indicator (currently an 8px accent dot inset in the filter button) and the
photo-avatar variant (already functional, ad-hoc styling). The design file now specifies both
(added 2026-07-08 via `plans/claude-design-brief-missing-elements.md`). This is a small,
surgical subtask.

**Depends on:** 1.1–1.3 and 2.3 (all done).

## Design source of truth (look this up — do not work from memory)

- Link: https://claude.ai/design/p/dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4?file=Skovens+Skatte.dc.html
- Fetch with the claude-design MCP tool: `mcp__claude-design__read_file`, `project_id: "dd75eb78-ea23-46b0-ba4b-7aeb33aff7e4"`, `path: "Skovens Skatte.dc.html"`. Content is HTML-entity-escaped and large; read in chunks as needed.
- **For this subtask, study:** in the floating top bar markup — the
  `<sc-if value="{{ filterActive }}">` dot (12px, `--accent`, 2px `--surface` border, hanging
  off the button's top-right corner at −3px/−3px) and the `<sc-if value="{{ avatarPhoto }}">`
  variant (48px circle, `object-fit: cover` image, same 2px `--pin-ring` border +
  `0 0 0 1px var(--line)` outer ring as the initial variant). Jump-rail states "Filter aktiv" /
  "Foto-avatar" preview these.

## Task

1. Update the filter button's active indicator to the design (size, offset outside the corner,
   surface-colored border so it reads against both the button and the map).
2. Align the photo-avatar variant with the design (identical ring treatment to the initial
   variant; image cover-fit). Keep the initial fallback and the dropdown menu untouched.
3. Both themes.

## Files

- `src/components/TopBar.tsx`

## Acceptance criteria

- `npm run build` + `npm run lint` pass.
- With filters narrowing results, the dot renders per design in both themes; gone when all
  types are active.
- Users with and without avatar photos both render per design; menu still opens.

## Final step

When all acceptance criteria pass, mark this subtask as done in the master plan: open
`plans/new-design-migration.md`, find this subtask's row in the "Subtask files" table, and set its
Status cell to `✅ Done (<today's date>)`.
